//
class MeetupSettings {
  final bool enableVerifiedUserOption;

  /// [enableVerifiedUserOption] by default meetupupdate screen display verified user option
  const MeetupSettings({
    this.enableVerifiedUserOption = true,
  });
}
