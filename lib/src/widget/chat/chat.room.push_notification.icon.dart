// import 'package:fireflutter/src/service/user.settings.dart';
import 'package:flutter/material.dart';

class ChatRoomPushNotificationIcon extends StatelessWidget {
  const ChatRoomPushNotificationIcon({
    super.key,
    required this.roomId,
  });
  final String roomId;
  @override
  Widget build(BuildContext context) {
    // final ref = UserSettings().col.doc(roomId);

    return Container();
  }
}
