/// Specifies the source where the picked file should come from.
enum MediaSource {
  /// Opens up the device camera, letting the user to take a new picture.
  camera,

  /// Opens the user's photo gallery.
  gallery,

  /// Opens up the File Picker
  file,
}
